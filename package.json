{
  "name": "sessvm",
  "version": "1.0.1",
  "description": "Server-owned session management library.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "files": [
    "dist",
    "client"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js"
    },
    "./client": {
      "types": "./client/refresh.d.ts",
      "default": "./client/refresh.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test:runtime": "node --test tests/runtime.test.js tests/client.test.mjs",
    "test": "npm run typecheck && npm run test:runtime"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/radiiplus/sessvm.git"
  },
  "keywords": [
    "session",
    "authentication",
    "typescript",
    "security",
    "csrf",
    "fingerprinting",
    "nodejs",
    "jwt",
    "server-owned"
  ],
  "author": "radiiplus",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "typescript": "^5.6.0"
  }
}